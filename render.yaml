services:
  - type: web
    name: delivery-system-do
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: NODE_VERSION
        value: 20
      - key: SQLITE_PATH
        value: /tmp/delivery-db.sqlite
